import * as math from './math';

describe('math', () => {
  test('Call math.add', () => {});
  test('Call math.subtract', () => {});
  test('Call math.multi', () => {});
  test('Call math.divide', () => {});
});
